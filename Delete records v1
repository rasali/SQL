DELETE FROM TABLE_NAME
WHERE ROWID IN (
SELECT RID FROM (
       SELECT /*+PARALLEL(10)*/
             ROWID RID,
             ROW_NUMBER() OVER(PARTITION BY COLUMN1, COLUMN2, COLUMNN ORDER BY ROWID) RN
       FROM TABLE_NAME
)
WHERE RN <> 1
)
